#define LoadReinforcements(unitPtr) "SMOV 0x2 (unitPtr|0x8000000); CALL $9EE274"
#define GiveItem(unit, item) "SVAL s3 item; GIVEITEM unit"
#define EndConvo "CALL $9EE2C4"
#define MakeBossMove(unit) "SVAL 1 0; CHAI unit